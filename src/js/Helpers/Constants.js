const GRAVITY = 3e-5; // N * m^3 / kg^3
const PLANET_SPRING = 1e2; // N / m
const MIN_DISTANCE = 2e-0; // m
const TIME_SCALE = 1e-5 // 1/s

export {
  GRAVITY,
  PLANET_SPRING,
  MIN_DISTANCE,
  TIME_SCALE,
}
