const GRAVITY = 3e-4; // N * m^3 / kg^3
const PLANET_SPRING = 1e0; // N / m
const MIN_DISTANCE = 2e-0; // m
const TIME_SCALE = 8e-4; // 1/s
const ORBITAL_SPEED = 1.1;

export {
  GRAVITY,
  PLANET_SPRING,
  MIN_DISTANCE,
  TIME_SCALE,
  ORBITAL_SPEED,
}
